{% set name = "DinoKnot" %}
{% set version = "0.0.1" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
    git_url: https://github.com/HosnaJabbari/DinoKnot.git

requirements:
  build:
    - cmake
    - {{ compiler('cxx') }}
